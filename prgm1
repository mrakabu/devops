//Introduction to Maven and Gradle: Overview of Build Automation Tools, Key   Differences Between Maven and Gradle, Installation and Setup
Sudo apt update
 Sudo apt install open jdk_17_jdk 
 Sudo apt install eclipse
 Sudo apt install maven -y 
 mvn -version
 wget -c https://services.gradle.org/distributions/gradle-7.4.2bin.zip -P /tmp 
 Sudo apt install unzip 
 unzip -d /opt/gradle /tmp/gradle-*.zip 
 ls/opt/gradle 
 sudo nano/etc/profile.d/gradle.sh 
 a new window will open then type export 
 PATH=/opt/gradle/gradle-7.4.2/bin:$PATH 
 then save and exit the file. This script will be sourced at shell startup
 chmod +x /etc/profile.d/gradle.sh
 source /etc/profile.d/gradle.sh 
 Gradle -v
 
